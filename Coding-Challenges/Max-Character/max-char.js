// abccccc === "c"
// apple123111 === "1"

/**
 * The solution used here can be used for other similar string questions (counting/comparing characters) like:
 * 1. Most common character in the string (max-char)
 * 2. Does string A has same characters as string B (anagram)
 * 3. Does string has any repeated characters in it?
 */

// Solution:
// Convert the string into object where keys are characters and values are number of repetitions